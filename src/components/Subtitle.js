import styled from "styled-components";
import { colors } from "../styles/variables";

export const WhiteSubtitle = styled.h2`
  font-size: 1.5rem;
  color: ${colors.white};
  font-weight: normal;
`;

export const DarkSubtitle = styled.h2`
  font-size: 1.5rem;
  color: ${colors.black};
  font-weight: normal;
`;